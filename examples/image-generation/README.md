## Image Generation

Generates an image from a text prompt using `OpenAiImageClient` and saves it to disk.

### Requirements

Set env variables and run:

```shell
export API_URL="https://api.openai.com/v1"
export API_KEY="sk-proj-xxxxxxxxxxxxxxxxxxxxxxxx"
export MODEL_ID="dall-e-3"
cargo run
```
